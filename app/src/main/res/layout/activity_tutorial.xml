<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <android.support.v4.view.ViewPager
            android:id="@+id/viewpager_custom"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <me.relex.circleindicator.CircleIndicator
            android:id="@+id/indicator_custom"
            app:ci_width="10dp"
            app:ci_height="4dp"
            app:ci_margin="6dp"
            app:ci_animator="@anim/anim_indicator"
            app:ci_animator_reverse="@anim/anim_indicator_reverse"
            app:ci_drawable="@drawable/black_radius_square"
            android:layout_centerInParent="true"
            android:layout_alignParentBottom="true"
            android:layout_width="fill_parent"
            android:layout_height="40dp"/>
<Button
    android:id="@+id/B_skip_tutorial"
    android:layout_width="80dp"
    android:layout_height="50dp"
    android:text="skip"
    android:textColor="@color/teala400"
    android:background="@color/transparent"
    android:layout_alignParentBottom="true"
    android:layout_alignParentStart="true"
    />
        <Button
            android:id="@+id/B_next_tutorial"
            android:layout_width="80dp"
            android:layout_height="50dp"
            android:text="다음"
            android:textColor="@color/teala400"
            android:background="@color/transparent"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            />
    </RelativeLayout>



</LinearLayout>